#include "util.h"

#include <array>
#include <cmath>
#include <iostream>

namespace util
{

Note::Note(float f)
    :freq(f)
{

}

const Note A  (440.00);
const Note Ais(466.16);
const Note B  (493.88);
const Note C  (523.25);
const Note Cis(554.37);
const Note D  (587.33);
const Note Dis(622.25);
const Note E  (659.25);
const Note F  (698.46);
const Note Fis(739.99);
const Note G  (783.99);
const Note Gis(830.61);

}
